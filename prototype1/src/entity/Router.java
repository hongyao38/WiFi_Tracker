package entity;

public class Router {
    
    // Location
    public int x;
    public int y;
    
    // Router properties
    private double frequency = 2.4;
    private double speed;
        // And so on lah hor, we can add as we need

    public Router(int x, int y) {
        this.x = x;
        this.y = y;
    }
}
